{
  "name": "leanmdblueprint",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "node --test dist/test/*.test.js",
    "dev:check": "tsc -p tsconfig.json --noEmit --watch"
  },
  "dependencies": {
    "markdown-it": "^14.1.0",
    "yaml": "^2.8.0"
  },
  "devDependencies": {
    "@types/markdown-it": "^14.1.2",
    "@types/node": "^24.0.0",
    "typescript": "^5.8.3"
  }
}
